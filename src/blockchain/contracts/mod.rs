use alloy::sol;

sol! {
    #[sol(rpc)]
    AlgebraV3Pool,
    "contracts/ABI/AlgebraV3.json"
}

sol! {
    #[sol(rpc)]
    AlgebraTwoSideFee,
    "contracts/ABI/AlgebraTwoSideFee.json"
}

sol! {
    #[sol(rpc)]
    IV2PairUint256,
    "contracts/ABI/IV2PairUint256.json"
}

sol! {
    #[sol(rpc)]
    IUniswapV2Pair,
    "contracts/ABI/IUniswapV2Pair.json"
}

sol! {
    #[sol(rpc)]
    CLPPool,
    "contracts/ABI/CLPPool.json"
}

sol! {
    #[sol(rpc)]
    IUniswapV3Pool,
    "contracts/ABI/IUniswapV3Pool.json"
}

sol! {
    #[sol(rpc)]
    IERC20,
    "contracts/ABI/IERC20.json"
}

sol! {
    #[sol(rpc)]
    IPancakeV3Pool,
    "contracts/ABI/IPancakeV3Pool.json"
}

sol! {
    #[sol(rpc)]
    ISwapRouter,
    "contracts/ABI/ISwapRouter.json"
}

sol! {
    #[sol(rpc)]
    IQuoter,
    "contracts/ABI/IQuoter.json"
}

sol! {
    #[sol(rpc)]
    IQuoterV2,
    "contracts/ABI/IQuoterV2.json"
}

sol! {
    #[sol(rpc)]
    IQuoterAlgebra,
    "contracts/ABI/IQuoterAlgebra.json"
}

sol! {
    #[sol(rpc)]
    IQuoterAlgebraSei,
    "contracts/ABI/IQuoterAlgebraSei.json"
}

sol! {
    #[sol(rpc)]
    IAlgebraPoolSei,
    "contracts/ABI/IAlgebraPoolSei.json"
}

sol! {
    #[sol(rpc)]
    IUniversalRouter,
    "contracts/ABI/IUniversalRouter.json"
}

sol! {
    #[sol(rpc)]
    IERC4626,
    "contracts/ABI/IERC4626.json"
}

sol! {
    #[sol(rpc)]
    IVerioIP,
    "contracts/ABI/IVerioIP.json"
}
